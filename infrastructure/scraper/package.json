{
  "name": "lct-capture",
  "version": "1.0.0",
  "description": "Stage 3 capture: Playwright page capture for the LCT acquisition pipeline (capture_discovery.mjs / capture_drive.mjs).",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test *.test.mjs"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
