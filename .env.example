# PostgreSQL Database Configuration
# Copy this file to .env and update with your settings

# Database connection settings
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=learning_connection_time
POSTGRES_USER=lct_user
POSTGRES_PASSWORD=lct_password

# For local development (non-Docker), use:
# POSTGRES_HOST=localhost
# POSTGRES_USER=your_local_user
# POSTGRES_PASSWORD=your_local_password

# Note: When using docker-compose, the host should be 'localhost' from outside the container
# or 'postgres' from within the Docker network
